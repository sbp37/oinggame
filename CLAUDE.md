## 작업 규칙
- artifact 생성 금지. 미리보기용 artifact를 절대 만들지 말 것.
- 코드 수정은 파일을 직접 편집하고, 결과는 텍스트로만 보고할 것.
- 시각적 확인이 필요하면 파일 경로와 변경 내용만 설명할 것.
- 게임 안 '업데이트 내역'(UPDATE_VERSION / SEEN_KEY / NEW 뱃지)은 사용자가 시킬 때만 쓴다.
  코드를 고쳤다고 해서 알아서 새 항목을 추가하지 말 것.
- version.json 의 notifyBuild(상단 새로고침 배너)는 사용자가 "크게 바뀌었다"고 판단해
  지시할 때만 올린다. 평소 수정은 build 만 올려 조용히 반영되게 둔다.

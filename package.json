{
  "name": "oing-firestore-rules-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Firestore security rules regression tests for oing-game (emulator-based, no deploy).",
  "scripts": {
    "guard": "node test/guard-no-direct-score-write.mjs",
    "pretest": "npm run guard",
    "test": "firebase emulators:exec --only firestore --project demo-oing-rules \"node --test test/firestore-rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.1.0",
    "firebase-tools": "^13.29.1"
  }
}
